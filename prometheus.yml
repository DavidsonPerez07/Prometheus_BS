global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'springboot'

    scheme: https

    metrics_path: /actuator/prometheus

    static_configs:
      - targets:
          - backendblashape-production.up.railway.app
